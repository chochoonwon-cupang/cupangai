# Supabase
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VM_TOKEN=

# 워커 모드
WORKER_MODE=server
WORKER_NAME=vm-001
WORKER_DEFAULT_JOB=placeholder
WORKER_REQUEUE_INTERVAL_SEC=60
WORKER_STUCK_REQUEUE_SEC=600
WORKER_HEARTBEAT_SEC=30
WORKER_HEARTBEAT_EVERY_SEC=30
WORKER_LEASE_SEC=420
WORKER_CLAIM_INTERVAL_SEC=2
WORKER_MAX_CONCURRENCY=1

# 워커 루프 (레거시 호환)
WORKER_SLEEP_SEC=3
WORKER_POLL_INTERVAL=5

# VM 카페 포스팅 (run_default_job)
# COMM_USERNAME: users 테이블 username — 쿠팡 API 키 조회용 (vm_accounts 계정별 쿠팡 키 없을 때)
# COMM_USERNAME=
# GEMINI_API_KEY=  # app_links에 없을 때 fallback
